# Test whether results of reflectProgram.em are any different if it is loaded as a package rather than a program
# NOT DIRECTLY EXECUTED BY REGRESSION TESTS

println "Loading reflectPackage.em"

directory.reflectProgram.testCallback
